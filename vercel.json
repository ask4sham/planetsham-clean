{
  "functions": {
    "api/publish/route.ts": {
      "runtime": "edge"
    }
  },
  "cron": [
    {
      "path": "/api/publish",
      "schedule": "*/5 * * * *"  // every 5 minutes
    }
  ]
}

// trigger redeploy
